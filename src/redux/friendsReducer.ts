import { ActionType, FriendsPageType } from "./state";

export const friendsReducer = (state: FriendsPageType, action: ActionType): FriendsPageType => {
	switch (action.type) {

		default:
			return state
	}
}